# Archived Implementation Plans

These plans are retained as historical context for earlier implementation work.
They are not authoritative documentation and may describe obsolete paths,
defaults, architecture, or user interfaces.

Use the repository root `README.md` for current user documentation and
`AGENTS.md` for current contributor guidance. The files under `prompts/` are
runtime contracts rather than documentation.
